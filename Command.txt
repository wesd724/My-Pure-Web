git remote show origin
git remote -v
git config --list

  '/'   -> 가장 최상의 디렉토리로 이동한다.(Web root)
 './'   -> 파일이 현재 디렉토리를 의미한다.
  '../' -> 상위 디렉토리로 이동한다.

- 만약 두단계 상위 디렉토리로 이동하려면
   '../../' 이렇게 사용하면 된다.